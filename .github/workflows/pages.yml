name: progress-site

on:
  push:
    branches: [main]
    paths:
      - "pipeline/data/roster/**"
      - "pipeline/data/analysis/**"
      - "pipeline/out/**"
      - "pipeline/progress.py"
      - "pipeline/cli.py"
      - ".github/workflows/pages.yml"
  workflow_dispatch:
    inputs:
      session:
        description: "Sesión UNGA (80 o 81)"
        required: false
        default: "80"

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Generar sitio de avance
        env:
          GITHUB_REPO: ${{ github.repository }}
          GITHUB_BRANCH: ${{ github.ref_name }}
        run: |
          SESSION="${{ github.event.inputs.session || '80' }}"
          python -m pipeline progress-site --session "$SESSION"

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
