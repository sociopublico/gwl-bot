# Snapshots de análisis (Claude → Sheets + disco)

Un JSON por discurso tras `python -m pipeline analyze …`.

```text
pipeline/data/analysis/<session>/<YYYY-MM-DD>/<slug>.json
```

Los usa `progress-site` para el dashboard de GitHub Pages (summary/notes incluidos).
